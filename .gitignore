# build output
build/
dist/
.venv/
*.spec
__pycache__/
*.pyc

# generated
SPIranha.ico
tests/work/
tests/work-gui/
tests/short.rom

# Built, not committed: flashrom is GPL and shipping the binary would mean
# shipping its corresponding source, which is far too big for this repository.
# See flashrom/PROVENANCE.md.
flashrom/flashrom.exe

# The programmer firmware IS committed, together with the source it was built
# from (firmware/pico-serprog/ plus the patch), so the GPL is satisfied.

# BIOS images: the dump of one specific board, 16 MiB each
*.rom
*.bin
*.fd

# system clutter
Thumbs.db
desktop.ini
config-for-tests/
tests/config-for-tests/
